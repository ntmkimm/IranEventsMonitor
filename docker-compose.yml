services:
  tactical-dashboard:
    build: .
    container_name: mini-world-monitor
    ports:
      - "3000:3000"
      - "5173:5173"
    volumes:
      - .:/app         
      - /app/node_modules 
    environment:
      - NODE_ENV=development